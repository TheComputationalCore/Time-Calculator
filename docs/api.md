# API Reference

## `add_time(start_time, duration, start_day=None)`

Add a duration to a start time in 12-hour format, optionally rolling over days and updating the weekday.

---

## 📌 Function Signature

```python
add_time(start_time: str, duration: str, start_day: str | None = None) -> str
